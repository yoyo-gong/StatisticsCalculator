/**
 * The {@code StatisticsCalculatorService} interface defines the contract for the service layer that performs statistical
 * calculations based on user input.
 */

public interface StatisticsCalculatorService {
    String calculateStatistics(String inputData);
}
